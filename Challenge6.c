#include <stdio.h>

int main(){
    int a, b;
    printf("Enter first num : ");
    scanf("%d", &a);
    printf("Enter secend num : ");
    scanf("%d", &b);
    float s = a + b;
    float m = a - b;
    float 
}